global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'dim-e2e'

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

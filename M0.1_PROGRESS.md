# M0.1 Progress Tracker

**Status:** In progress  
**Target:** Walking skeleton (end-to-end message flow)

## Subtasks

| # | Task | Deps | Status | Exit Criteria |
|---|------|------|--------|---------------|
| 1 | JSONata library spike | — | ⏳ Next | One library selected, findings recorded |
| 2 | Message envelope + Channel | — | ⏳ Queued | Unit tests pass; channel backpressure works |
| 3 | Executor (single-worker) | 2 | ⏳ Queued | Message enqueued → dequeued exactly once |
| 4 | HTTP source adapter | 2 | ⏳ Queued | HTTP webhook → message on ingress channel |
| 5 | File sink adapter | 2 | ⏳ Queued | Channel → file write, contents match |
| 6 | Filter step | 1,2 | ⏳ Queued | Fixture test: passing/failing predicate |
| 7 | Translate step | 1,2 | ⏳ Queued | Fixture test: JSONata → transformed output |
| 8 | Route schema + config loader | — | ⏳ Queued | `midctl validate` accepts/rejects |
| 9 | Error path (dead-letter) | 2,7 | ⏳ Queued | Failed message → dead-letter sink |
| 10 | `midctl run` / `validate` CLI | 3-9 | ⏳ Queued | End-to-end: HTTP → filter → translate → file |

## Critical Path

1. **M0.1.1** (JSONata) — 15-30 min spike
2. **M0.1.2** (Channel/Envelope) — 1-2 hours
3. **M0.1.8** (Schema/Config) — 1-2 hours (can run in parallel with 1-2)
4. **M0.1.3-5, M0.1.6-9** (Adapters & Steps) — 3-5 hours (each depends on 1-2)
5. **M0.1.10** (CLI) — 30 min (runs the above)

## Notes

- M0.1.1 spike will select JSONata library and document any known gaps
- All tests use `-race` flag from M0.2; M0.1 builds deterministic single-threaded baseline
- Fixtures live in `test/fixtures/` and use YAML files defining input→expected output

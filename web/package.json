{
  "name": "dim-studio",
  "version": "1.0.0",
  "description": "Visual route-authoring interface for dim middleware",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "reactflow": "^11.10.0",
    "@monaco-editor/react": "^4.5.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/user-event": "^14.0.0",
    "vite": "^4.4.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vitest": "^0.34.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run embed",
    "embed": "mkdir -p ../cmd/dimctl/web_dist && cp -r dist/* ../cmd/dimctl/web_dist/",
    "preview": "vite preview",
    "test": "vitest"
  }
}
